import { EmailMatchesUserGuard } from './email-matches-user.guard';

describe('EmailMatchesUserGuard', () => {
  it('should be defined', () => {
    expect(new EmailMatchesUserGuard()).toBeDefined();
  });
});
